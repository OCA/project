This module requires the `PyGithub` Python library:

    pip install PyGithub

Latest stable release the module has been tested against:
`PyGithub` 2.9.1 (older releases down to 2.8.1 are known to work).
