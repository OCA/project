This module has no Python dependency of its own, but it does nothing on
its own: install one bridge module per platform you want to connect
(`project_github` for GitHub, `project_gitlab` for GitLab). Each bridge
installs the Python library of its platform.
