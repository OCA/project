<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.form.inherit.convert.restrict</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="project.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//app[@name='project']" position="inside">
                <block title="Task to Ticket Conversion" id="convert_restrict">
                    <setting
                        id="restrict_ticket_conversion_setting"
                        help="Only tasks whose project is visible to invited portal users can be converted into helpdesk tickets."
                    >
                        <field name="restrict_ticket_conversion" />
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
