The `project_helpdesk` bridge lets managers convert a project task into a
helpdesk ticket. A helpdesk ticket can then be shared publicly through its own
portal share link, which is a way to expose data of a task whose project was
never meant to be visible to portal users.
