Go to Settings > Project and enable **Restrict task-to-ticket conversion to
portal projects** for the current company.

The setting is stored on the company, so in a multi-company database each
company can enable it independently.
