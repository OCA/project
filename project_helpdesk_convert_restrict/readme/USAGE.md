With the setting enabled:

- Open a task whose project privacy is *Invited portal users …* and use
  Action > Convert to Ticket: the conversion wizard opens as usual.
- Do the same on a task of an *All Employees* / *Private* project: an error is
  raised and no ticket is created.
- Converting several tasks at once is blocked as soon as one of them belongs to
  a non-portal project.
