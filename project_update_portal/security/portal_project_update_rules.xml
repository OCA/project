<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Portal users can see project updates if they follow the project or the update -->
    <record id="project_update_rule_portal" model="ir.rule">
        <field
            name="name"
        >Project Update: portal users: following project or update</field>
        <field name="model_id" ref="project.model_project_update" />
        <field name="domain_force">[
            ('project_id.privacy_visibility', '=', 'portal'),
            '|',
                ('project_id.message_partner_ids', 'child_of', [user.partner_id.commercial_partner_id.id]),
                ('message_partner_ids', 'child_of', [user.partner_id.commercial_partner_id.id])
        ]</field>
        <field name="perm_read" eval="True" />
        <field name="perm_write" eval="False" />
        <field name="perm_create" eval="False" />
        <field name="perm_unlink" eval="False" />
        <field name="groups" eval="[(4, ref('base.group_portal'))]" />
    </record>
</odoo>
