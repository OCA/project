This module allows to assign Projects to several managers.
This is needed when we want to assign specific access rules based on this field
It is meant to be inherited by a custom module with custom security rules
For example, we might want to allow only managers to create tasks in a given project
In this case, inherit this module and create the right security rule to do so.
