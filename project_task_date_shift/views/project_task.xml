<odoo>
  <!-- List View -->
  <record id="project_task_date_shift_view_tree" model="ir.ui.view">
    <field name="name">project.task.date.shift.tree</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_tree2"/>
    <field eval="2" name="priority"/>
    <field name="arch" type="xml">
      <field name="user_id" position="after">
        <field name="date_start"/>
        <field name="date_end"/>
      </field>
    </field>
  </record>

  <!-- Form View -->
  <record id="project_task_date_shift_view_form" model="ir.ui.view">
    <field name="name">project.task.date.shift.form</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_form2"/>
    <field eval="2" name="priority"/>
    <field name="arch" type="xml">
      <data>
        <xpath expr="//field[@name='date_deadline']" position = "before">
          <field name="date_start"/>
          <field name="date_end"/>
        </xpath>
      </data>
    </field>
  </record>

  <!-- Gantt View -->
  <record id="project_task_date_shift_view_gantt" model="ir.ui.view">
    <field name="name">project.task.date.shift.gantt</field>
    <field name="model">project.task</field>
    <field name="arch" type="xml">
      <gantt date_start="date_start" date_stop="date_end" progress="progress">
      </gantt>
    </field>
  </record>

  <!-- actions opening views on models -->
  <record id="project.act_project_project_2_project_task_all" model="ir.actions.act_window">
    <field name="name">Tasks</field>
    <field name="res_model">project.task</field>
    <field name="view_mode">kanban,tree,form,calendar,pivot,graph,activity,gantt</field>
  </record>
</odoo>
