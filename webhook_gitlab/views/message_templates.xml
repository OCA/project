<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <template id="no_task_reference_in_title" name="Git: no task reference in title">
        <t
        >**WARNING**: Could not link this PR/MR to an Odoo task: no task reference found.

Supported references, in the PR/MR title, in the source branch name or in the commit messages:

- explicit task id: `taskid#123` or `tid#123` (case insensitive)

- task key matching the task name by configured regex (e.g. `ABC-123`, uppercase)
        </t>
    </template>

</odoo>
