<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <template id="no_task_reference_in_title" name="Git: no task reference in title">
        <t>**WARNING**: Could not link this PR/MR to an Odoo task: no task reference found.

Supported references:

- explicit task id in the title: `task#123` or `t#123` (case insensitive)

- task reference in the title or in the source branch name, matching the task title by configured regex (e.g. `ABC-123`)
        </t>
    </template>

</odoo>
